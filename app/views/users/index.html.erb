<section>
  <h2>user#profile</h2>

	<% @users.each do |user| %>
		<%= user.name %>
		<%= user.street_address %>
		<%= user.city %>
		<%= user.state %>
		<%= user.zip %>
		<%= user.email %>
	<% end %>
</section>
